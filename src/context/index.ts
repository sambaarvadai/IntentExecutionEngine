// ------------------------------------------------------------------
// Context Module Public Interface
// ------------------------------------------------------------------

// Export all types for external modules
export * from './types';

// Context creation and management utilities will be added here
// when we implement the actual context management logic

// For now, this serves as the public interface following rule #1
// Other modules should only import from this index.ts file
